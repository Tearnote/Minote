Try some gcc sanitizers
Try valgrind
Refactor repetitive parts of rendering
Refactor file operations (fclose can return a write error!)
Package licenses and readme
Make libraries use our own malloc when possible
Add some more logging
Move GM requirements into game state
Make some more trivial mutexes into atomics
Change grade from a string to an enum with a string lookup table in render thread
Make playfield an opaque struct with accessors for its contents
Make rotation system into a function
Replace #defines with consts
Switch from specialized drawcalls to generic triangle and mesh renderers
Turn the effect system from a clusterfuck into an Observer pattern
Turn the ease system from a singleton into state struct + function(state, *var)
Use unions instead of void* argument where possible
Typedef the structs
Use coord struct everywhere, with help of compound literals
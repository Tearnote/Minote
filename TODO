Try some gcc sanitizers
Try valgrind
Refactor repetitive parts of rendering
Refactor file operations (fclose can return a write error!)
Package licenses and readme
Make libraries use our own malloc when possible
Add some more logging
Move GM requirements into game state
Make some more trivial mutexes into atomics
Change grade from a string to an enum with a string lookup table in render thread
Make playfield an opaque struct
Replace #defines
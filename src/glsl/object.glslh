layout(binding = 0) uniform World {
	mat4 view;
	mat4 projection;
	mat4 viewProjection;
} world;

struct Instance {
	mat4 transform;
	vec4 tint;
	float roughness;
	float metalness;
	float pad0;
	float pad1;
};

layout(std430, binding = 1) readonly buffer Instances {
	Instance data[];
} instances;
